export type MinifigType = {
  name: string;
  picture: string | null;
  detailsUrl: string;
  setId: string;
}

export type MinifigPartType = {
  id: string;
  name: string;
  picture: string | null;
}
